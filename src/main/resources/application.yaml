spring:
  application:
    name: challenge-java-developer
  jpa:
    defer-datasource-initialization: true
    

server:
  port: 5000  
  
  datasource:
    url: jdbc:h2:file:mem/test
    driver-class-name: org.h2.Driver
    username: sa
    password: password

  h2:
    console:
      enabled: true
      path: /h2-console
      